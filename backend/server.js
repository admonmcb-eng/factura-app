 process.on('exit', (code) => console.log('EVENTO exit, code:', code)); process.on('SIGTERM', () => { console.log('EVENTO SIGTERM'); process.exit(0); }); process.on('SIGINT', () => { console.log('EVENTO SIGINT'); process.exit(0); }); process.on('uncaughtException', (err) => { console.error('uncaughtException:', err); }); process.on('unhandledRejection', (err) => { console.error('unhandledRejection:', err); }); require('dotenv').config(); console.log('Arrancando servidor...'); const express = require('express'); const cors = require('cors'); const path = require('path'); const fs = require('fs'); const app = express(); app.use(cors()); app.use(express.json()); app.get('/api/health', (req, res) => res.json({ ok: true })); // Arrancamos a escuchar en el puerto INMEDIATAMENTE, antes de tocar la base // de datos, para que la plataforma de despliegue detecte cuanto antes que // la app ya está viva. Las rutas reales se agregan justo después. const PORT = process.env.PORT || 4000; app.listen(PORT, '0.0.0.0', () => { console.log(`Servidor escuchando en el puerto ${PORT}`); app.use('/api/auth', require('./routes/auth')); app.use('/api/company', require('./routes/company')); app.use('/api/clients', require('./routes/clients')); app.use('/api/products', require('./routes/products')); app.use('/api/invoices', require('./routes/invoices')); app.use('/api/payments', require('./routes/payments')); app.use('/api/expenses', require('./routes/expenses')); app.use('/api/reports', require('./routes/reports')); // Sirve el frontend ya compilado (frontend/dist) cuando existe, para // desplegar backend + frontend como un solo servicio (ej. Railway). const frontendDist = path.join(__dirname, '..', 'frontend', 'dist'); if (fs.existsSync(frontendDist)) { app.use(express.static(frontendDist)); app.use((req, res, next) => { if (req.path.startsWith('/api')) return next(); res.sendFile(path.join(frontendDist, 'index.html')); }); } console.log('Rutas y base de datos inicializadas correctamente.'); });
