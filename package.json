{ "name": "factura-app", "version": "1.0.0", "private": true, "scripts": { "install:all": "npm install --prefix backend && npm install --prefix frontend", "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend", "start": "node -e \"require('http').createServer((req,res)=>res.end('ok')).listen(process.env.PORT||3000,'0.0.0.0',()=>console.log('SERVIDOR MINIMO ARRIBA'))\"" }, "engines": { "node": "22.x" } }
